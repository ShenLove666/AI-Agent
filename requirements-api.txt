# 云端 LLM + 本地 Embedding 模式 (LLM_PROVIDER=api)
# LLM 走 DeepSeek API；Embedding 使用项目内 BGE-small-zh-v1.5，CPU 可运行
# 不安装 OCR 等无关重型依赖
# 用法: uv pip install -r requirements-api.txt

# ---- Web 框架 ----
fastapi
uvicorn
python-multipart

# ---- 数据库 ----
sqlalchemy
alembic>=1.14,<2
pymysql

# ---- LLM / Vector ----
openai>=1.0
sentence-transformers>=2.3.0
pymilvus>=3.0.1
milvus-lite>=3.2.0
langgraph>=1.0,<2

# ---- 文档解析 (扫描件 OCR 需另装 paddleocr, 数字 PDF 不受影响) ----
pypdf
pandas
openpyxl
pymupdf
Pillow
numpy

# ---- 认证 ----
python-jose[cryptography]
passlib
bcrypt
argon2-cffi

# ---- 工具 ----
httpx
jieba
tiktoken
python-docx
