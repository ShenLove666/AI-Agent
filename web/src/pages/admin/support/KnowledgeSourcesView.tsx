import type { KnowledgeSource } from "@/services/supportService";
import { Badge } from "@/components/ui/badge";

export function KnowledgeSourcesView({ sources }: { sources: KnowledgeSource[] }) {
  if (!sources.length) return <div className="rounded-xl border border-dashed p-8 text-center text-sm text-slate-500">暂无可发布知识来源。</div>;
  return <section aria-labelledby="knowledge-sources-title" className="rounded-xl border bg-white p-5"><div className="flex items-center justify-between"><div><h2 id="knowledge-sources-title" className="font-semibold">知识来源与复核状态</h2><p className="mt-1 text-xs text-slate-500">官方摘要、商家 SOP 与用户上传内容分开标注。</p></div><Badge variant="outline">{sources.length} 份</Badge></div><div className="mt-4 grid gap-3 lg:grid-cols-2">{sources.map(source => <article key={source.id} className="rounded-lg border p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="text-sm font-semibold">{source.title}</h3><p className="mt-1 text-xs text-slate-500">{source.publisher || "虚构商家 SOP"} · {source.jurisdiction || "适用范围见下方"}</p></div><Badge className={source.contentOrigin === "public_summary" ? "bg-emerald-600" : "bg-amber-500 text-slate-950"}>{source.contentOrigin === "public_summary" ? "官方来源摘要" : "演示知识"}</Badge></div><div className="mt-3 flex flex-wrap gap-2 text-[11px] text-slate-500"><span>复核 {source.reviewStatus}</span><span>下次复核 {source.nextReviewAt || "随版本复核"}</span><span>SHA {source.checksum?.slice(0,10) || "不可用"}</span></div><p className="mt-3 text-xs text-slate-600">适用：{source.applicability.join("、") || "未提供"}</p><p className="mt-1 text-xs text-slate-600">排除：{source.exclusions.join("、") || "未提供"}</p>{source.canonicalUrl ? <a className="mt-3 inline-block text-xs font-medium text-blue-600 hover:underline" href={source.canonicalUrl} target="_blank" rel="noreferrer">查看权威原文 ↗</a> : <p className="mt-3 text-xs text-amber-700">无外部链接：该条为项目内商家 SOP。</p>}</article>)}</div></section>;
}
