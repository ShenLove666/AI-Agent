## 1. Provenance Schema and Migration

- [x] 1.1 Add data-source manifests, provenance/lineage fields, source-linked support fields, and knowledge freshness metadata in additive SQLAlchemy models plus Alembic `0006_retail_data_provenance`; truthfully backfill legacy generated price/time/channel/store values and preserve downgrade. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_migrations.py -q`; expected: upgrade, backfill, schema-signature, and downgrade tests all pass.
- [x] 1.2 Add typed provenance values, manifest validation, field-lineage validation, and ownership invariants. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_retail_provenance.py -q -k "manifest or lineage or ownership"`; expected: valid manifests pass and drift, false provenance, cross-owner links, and malformed lineage fail before mutation.

## 2. Verified Real-Data Snapshots

- [x] 2.1 Add a project-local normalizer that reads the user-provided GBK basket files without modifying them and emits deterministic UTF-8 gzip assets plus checksummed manifest. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_retail_provenance.py -q -k "local_basket or gbk or checksum"`; expected: 9,835 baskets, 43,367 lines, 169 products, 10 categories, stable bytes, and source-drift rejection.
- [x] 2.2 Add a reproducible UCI Online Retail II acquisition/normalization command and a committed bounded CC BY 4.0 snapshot with DOI, selection rules, dates, countries, quantities, prices, customer keys, and observed cancellations. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_retail_provenance.py -q -k "uci or cancellation or license"`; expected: pinned manifest/checksum and cancellation semantics pass without network.
- [x] 2.3 Extend commerce ingestion and reset to batch-load both managed snapshots, preserve null unavailable dimensions, expose source quality, and remain idempotent/fail-closed. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_retail_provenance.py tests/test_demo_seed.py -q -k "import or quality or idempotent or reset or ordinary"`; expected: source counts/limitations are truthful, repeat seed is stable, and ordinary records survive reset.

## 3. Authoritative Knowledge Corpus

- [x] 3.1 Create at least 12 original project knowledge summaries/decision tables with official-source or merchant-SOP metadata, applicability, exclusions, retrieval/effective dates, checksums, and review schedules. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_knowledge_sources.py -q -k "metadata or corpus or summary"`; expected: corpus size/coverage passes, canonical links are present, and copied/unattributed/invalid entries are rejected.
- [ ] 3.2 Enforce freshness, supersession, provenance separation, applicability conflicts, and mandatory attribution during knowledge release publication and retrieval. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_knowledge_sources.py tests/test_knowledge_release.py -q -k "freshness or publish or superseded or applicability or rollback"`; expected: only valid current snapshots publish/retrieve and rollback remains atomic.
- [ ] 3.3 Extend citation/API payloads with publisher, canonical URL, retrieval date, applicability, exclusions, and review status. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_support_api_contracts.py -q -k "citation or knowledge or source"`; expected: typed attribution is returned and missing/foreign sources are not exposed.

## 4. Grounded Support Scenario Scale

- [x] 4.1 Implement a preflighted deterministic generator for 300-500 source-linked cases across all required categories, lifecycle states, review decisions, and evidence outcomes. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_grounded_support_scenarios.py -q -k "scale or coverage or deterministic"`; expected: stable keys/content and every declared segment meets its minimum.
- [ ] 4.2 Enforce product/category/source constraints, fresh-food return boundaries, cancellation limitations, safety escalation, and field-level observed/derived/synthetic truth. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_grounded_support_scenarios.py -q -k "constraint or fresh or cancellation or safety or lineage"`; expected: impossible claims fail preflight and valid cases carry correct lineage.
- [x] 4.3 Integrate the expanded cases, decisions, gaps, and evaluation inputs into seed/reset with ownership closure and atomic recovery. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_demo_seed.py tests/test_grounded_support_scenarios.py -q -k "seed or reset or rollback or ownership"`; expected: initial/repeat/reset/fault paths pass and no ordinary data or external files are changed.

## 5. Provenance and Coverage Product Experience

- [ ] 5.1 Add ownership-scoped data-source, quality, case-provenance, support-coverage, and knowledge-source APIs while keeping existing clients compatible. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_support_api_contracts.py tests/test_api_contract_baseline.py -q`; expected: all new routes are authorized/typed and every active frontend call is present in OpenAPI.
- [ ] 5.2 Build the Data Sources and Knowledge Sources views plus case provenance drawer with loading/empty/error states, canonical links, limitations, lineage, freshness, and prominent demo labeling. Verify with `npm --prefix web test -- DataSources.test.tsx CaseProvenance.test.tsx KnowledgeSources.test.tsx`; expected: all focused UI suites pass including accessibility and unavailable-field states.
- [ ] 5.3 Update support reports and secondary basket insights to separate observed/derived/synthetic populations, reject unlabeled mixed KPIs, and show source/generator versions. Verify with `npm --prefix web test -- SupportOperations.test.tsx RetailOperations.test.tsx`; expected: demo/mixed states and provenance filters pass without breaking the legacy route.

## 6. Acceptance and Handoff

- [x] 6.1 Update README/data cards with source attribution, licenses, limitations, acquisition versus offline runtime commands, embedding fallback, and a source-linked interview walkthrough. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_retail_provenance.py tests/test_knowledge_sources.py -q -k "documentation or attribution"`; expected: documented manifests/links/commands match runtime contracts.
- [x] 6.2 Migrate and reseed the project-local demo database, then smoke-test login, source counts, at least 300 cases, knowledge coverage, provenance detail, evaluation gate, and reports without an LLM or embedding model. Verify with `.\.venv\Scripts\python.exe -m app.cli seed-demo --reset` followed by the documented smoke script; expected: all counts/coverage match manifests and repeated seed reuses stable records.
- [ ] 6.3 Run canonical verification and strict OpenSpec validation. Verify with `powershell -ExecutionPolicy Bypass -File scripts/verify.ps1` and `openspec validate ground-demo-in-real-retail-data --strict`; expected: both commands exit 0.
