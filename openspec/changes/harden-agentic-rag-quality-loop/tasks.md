## 1. Typed Agent Tool Runtime

- [x] 1.1 Add Pydantic tool-call/result contracts, owner-scoped execution context, registry lookup, safe validation/error mapping, and trace timing. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_agent_tools.py -q -k "contract or validation or error or trace"`; expected: valid calls return unified evidence, invalid input executes no handler, and traces contain safe status/duration/error fields.
- [x] 1.2 Implement granular read-only knowledge search/document, commerce association/product-metric, and support case/quality/gap tools with temporary coarse-name aliases. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_agent_tools.py -q -k "knowledge or commerce or support or alias"`; expected: every tool returns source-tagged evidence, owner filtering hides foreign records, and existing aliases remain functional.

## 2. Evidence-Driven Re-planning

- [x] 2.1 Replace ad-hoc planner JSON with validated plans containing typed calls, rationale, runtime mode, plan history, reviewer feedback, and tool errors while preserving deterministic fallback. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_agentic_rag.py -q -k "plan or fallback or model"`; expected: malformed model output safely falls back and both planners emit only registered typed calls.
- [x] 2.2 Route insufficient/conflicting evidence and recoverable tool failure back through the planner, enforce plan/tool budgets, and persist explicit terminal state. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_agentic_rag.py -q -k "replan or budget or terminal or insufficient"`; expected: the planner is invoked twice, the second strategy differs or terminates safely, and no path exceeds configured limits.

## 3. Real Evaluation Execution and Scoring

- [x] 3.1 Add a real evaluation runner that invokes the shared agent runtime per immutable case, constructs answers only from runtime evidence, records latency/runtime mode/terminal state/tool calls/evidence, and never passes or copies the reference answer into generation. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_agent_evaluation_runtime.py -q -k "execution or reference or trace or latency"`; expected: recorded answers come from runtime output and reference-answer leakage tests pass.
- [x] 3.2 Add pure versioned scorers for expected points/evidence, citations, groundedness, refusal-escalation, and latency plus a risk-aware aggregate gate. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_agent_evaluation_runtime.py -q -k "score or grounded or refusal or gate"`; expected: fluent unsupported answers fail evidence dimensions and mandatory safety failures block the gate.
- [x] 3.3 Persist additive evaluation detail and configuration, update run/detail/overview contracts, enforce release/dataset ownership, and add Alembic `0007_agent_evaluation_runtime` only where explicit columns are required. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_migrations.py tests/test_support_quality.py tests/test_support_api_contracts.py tests/test_api_contract_baseline.py -q`; expected: upgrade/downgrade, owner isolation, failure states, legacy response compatibility, and additive detail fields all pass.

## 4. Demo Coverage and Acceptance

- [x] 4.1 Expand the managed evaluation catalog to at least 50 deterministic, source-linked cases spanning policy lookup, commerce analysis, support quality, insufficient evidence, cancellation limits, fresh-food boundaries, and safety escalation; keep reference answers scoring-only. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_demo_catalog.py tests/test_demo_seed.py tests/test_agent_evaluation_runtime.py -q -k "catalog or coverage or deterministic or seed"`; expected: segment minima, stable keys, repeat seed/reset, and offline ownership closure pass.
- [x] 4.2 Document the Agent Eval walkthrough, fallback/model-backed distinction, metric definitions, gate thresholds, and trace interpretation without overstating production quality. Verify with `.\.venv\Scripts\python.exe -m pytest tests/test_agent_evaluation_runtime.py -q -k "documentation"`; expected: documented commands/configuration match runtime contracts and required caveats are present.
- [x] 4.3 Run canonical verification and strict change validation. Verify with `powershell -ExecutionPolicy Bypass -File scripts/verify.ps1` followed by `openspec validate harden-agentic-rag-quality-loop --strict`; expected: both commands exit 0 with no secret, generated database, or unrelated file changes.
